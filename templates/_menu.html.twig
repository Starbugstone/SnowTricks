<nav id="topMenu" class="hide-on-small-only">
    <div class="nav-wrapper indigo darken-4">
        <a href="{{ path('home') }}" class="brand-logo left ml-2">
            <img id="menu-logo" src="{{ app_menu_logo }}" alt="SnowTricks Logo">
        </a>

        <ul class="right">
            <li><a href="{{ path('home') }}">Home</a></li>
            <li><a href="{{ path('trick.search') }}">Tricks</a></li>
            {% if is_granted('ROLE_USER') %}
                <li><a href="{{ path('trick.new') }}">New Trick</a></li>
                <li><a href="{{ path('app_logout') }}">Logout</a></li>
                <li><a href="{{ path('admin.profile') }}"><i class="material-icons" title="Profile">person</i></a></li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="/admin"><i class="material-icons" title="Admin">settings</i></a></li>
                {% endif %}
            {% else %}
                <li><a href="{{ path('app_login') }}">Sign In</a></li>
                <li><a href="{{ path('app_register') }}">Sign Up</a></li>
            {% endif %}
        </ul>
    </div>
</nav>


<nav id="mobileMenu" class="hide-on-med-and-up">
    <div class="nav-wrapper indigo darken-4">
        <div class="row">
            <div class="col s4 center-align">
                <a href="{{ path('home') }}"><i class="material-icons">home</i></a>
            </div>
            <div class="col s4 center-align">
                <a href="{{ path('trick.search') }}"><i class="material-icons">turned_in</i></a>
            </div>

            <div class="col s4 center-align">
                {% if is_granted('ROLE_USER') %}
                    <a href="{{ path('app_logout') }}"><i class="material-icons">exit_to_app</i></a>
                {% else %}
                    <a href="{{ path('app_login') }}"><i class="material-icons">person</i></a>
                {% endif %}
            </div>

        </div>

    </div>
</nav>