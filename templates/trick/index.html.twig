{% extends 'base.html.twig' %}

{% block title %}SnowTricks{% endblock %}

{% block body %}
    <div id="trickhomeContainer">
        <!-- Header image with slogan -->
        <div class="valign-wrapper vh100" id="st-header-image"
             style="background-image: url(https://images.pexels.com/photos/531880/pexels-photo-531880.jpeg)">

            <h1 class="header-text-title center-align w100 font-coiny text-shadow">Phrase Accroche</h1>

        </div>

        <!-- The tricks cards -->
        <div class="row mx-xl-5 mt-xl-5 mt-l-3 mt-s-1 flex-center" id="trick-card-list">
            {% include 'trick/_trick-card.html.twig' with {'tricks': tricks} %}
        </div>

        {% if nextPage > 0 %}
            <div class="center-align mb-4">
                <a href="{{ path('home', {page: nextPage,}) }}" class="btn-large" id="home-page-load-more">Load more</a>
            </div>
        {% endif %}

        {# TODO: Load More #}
    </div>
{% endblock %}
