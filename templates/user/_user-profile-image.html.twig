{% if user.image is not null %}
    {% set imageUrl = '/uploads/user_images/'~user.image %}
{% else %}
    {% set imageUrl = app_default_user_image %}
{% endif %}

<img src="{{ imageUrl }}" alt="{{ user.userName }} avatar" title="{{ user.username }}" class="circle responsive-img mr-3">