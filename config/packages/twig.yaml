twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        default_user_image: '%env(DEFAULT_IMAGE_PATH)%%env(DEFAULT_USER_IMAGE)%'

